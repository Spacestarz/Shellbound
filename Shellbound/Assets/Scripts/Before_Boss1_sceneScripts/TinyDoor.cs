using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TinyDoor : MonoBehaviour
{
    //this will be a tiny door going over the glove when its at the starpos so instead
    //of the glove just bam disappear a tiny "door" will go over it then the BIG door will open.
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
